dag.overwrite = true
report.overwrite = true
timeline.overwrite = true
docker.enabled = true
docker.fixOwnership = true
process.executor = 'local'
executor {
    $local {
    cpus = 96
    memory = '500 GB'
    }
}
